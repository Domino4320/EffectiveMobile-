Добавил отдельного пользователя для доступа к Бд (администрирование)
Настроил подключение к постгресу
Вынес роли в отдельную таблицу, т.к. это поможет в будущем если у пользователя может быть несколько ролей
Для безопасности срок истечения куки хранится и в бд и проверяется каждый раз, чтобы легко его можно было отозвать при взломе
Аутентификация с помощью токенов в БД и куки (дамми хэш для защиты от Timing атак)
Кастомный middleware с путями исключениями и получением токена и пользователя через cookie и обращение к БД 
Структура доступа